name: bmi_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  syncfusion_flutter_gauges: ^29.2.11

dev_dependencies:
  flutter_launcher_icons: ^0.14.4
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_app_icons:
  android: true
  ios: true
  image_path: "assets/icon/10476437.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
